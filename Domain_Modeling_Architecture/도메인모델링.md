# 도메인 모델링

- TDD와 호환이 잘되는 도메인 모델링

## 핵심 패턴

- 엔티티
- 값 객체
- 도메인 서비스

## 도메인 모델이란?

도메인이란 단어는 우리가 해결하려는 문제를 뜻한다. 

모델은 유용한 특성을 포함하는 프로세스나 현상의 지도를 뜻한다.

## 이 디렉토리에서 진행 할 내용

- 실제 세계의 비즈니스 로직을 도메인 모델링을 통하여 구현 할 것이다.
- 실제 전문가가 사용하는 용어와, 어떤 방식으로 비즈니스가 돌아가는지를 정리 한 이후, 이 현실세계의 작업을 코드로 구현하는 작업을 한다.
