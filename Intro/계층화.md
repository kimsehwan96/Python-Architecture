# 계층화

어떤 함수나 모듈, 객체인 A가 다른 함수나 모듈, 객체인 B를 사용하는 경우, A가 B에 의존한다 라고 표현한다.

이런 의존성은 네트워크나 그래프를 그리고, 큰 진흙 공에서는 의존성이 제어 할 수 없을 정도로 복잡하다.

계층화 하는 아키텍쳐는 이러한 문제를 해결하는 방법중 하나다. 

어떤 코드 범주가 어떤 코드 범주를 호출 할 수 있는지에 대한 규칙을 도입 하는 것

대표적인 예가 3 계층 아키텍쳐이다.

1. 표현계층

2. 비즈니스 로직

3. 데이터베이스 계층
